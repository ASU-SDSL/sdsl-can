if(CONFIG_SDSL_CAN)
  zephyr_library()
  zephyr_library_sources(src/can_link.c)
  zephyr_library_include_directories(include)
  zephyr_library_include_directories(${CMAKE_CURRENT_BINARY_DIR}/proto)
  zephyr_include_directories(include)
  zephyr_include_directories(${CMAKE_CURRENT_BINARY_DIR}/proto)

  list(APPEND CMAKE_MODULE_PATH ${ZEPHYR_BASE}/modules/nanopb)
  include(nanopb)
  zephyr_nanopb_sources(${ZEPHYR_CURRENT_LIBRARY} proto/link.proto)
endif()
